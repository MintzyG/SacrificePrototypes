#pragma once
#include "cards.hpp"

// Returns a string literal representation of the 'PlusType' enum value
const char* StringPlusType(PlusType);

// Returns a string literal representation of the 'Color' enum value
const char* StringColor(Color);

// Returns a string literal representation of the 'SpecialType' enum value
const char* StringSpecial(SpecialType);

// Returns a string literal representation of the 'Type' enum value
const char* StringType(Type);